apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "hardware-under-maintenance.fullname" . }}-test-connection"
  labels:
    {{- include "hardware-under-maintenance.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "hardware-under-maintenance.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
