instances: ["test"]
entrypoints:
  - path: "/app/src/ska_te_mid_skysimctl/slice_of_pi/MidItfPi0.py"
    name: "MidItfPi0.MidItfPi0Device"
server:
  instances:
    - name: "test"
      classes:
        - name: "MidItfPi0Device"
          devices:
            - name: "skysim/ctl/2"
image:
  registry: "{{ .Values.image.registry }}"
  image: "{{ .Values.image.image }}"
  tag: "{{ .Values.image.tag }}"
  pullPolicy: "{{ .Values.image.pullPolicy }}"
