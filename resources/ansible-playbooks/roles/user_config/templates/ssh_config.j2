{% for item in all_hosts %}
    {% if item.name not in group_names %}
Host {{ item.name }}
    HostName {{ item.ip }}   
    User {{ ssh_keys_user_name }}
    ForwardAgent yes
    {% endif %}

{% endfor %}

