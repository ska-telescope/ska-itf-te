apiVersion: v1
kind: Service
metadata:
  name: {{ include "hardware-under-maintenance.fullname" . }}
  labels:
    {{- include "hardware-under-maintenance.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "hardware-under-maintenance.selectorLabels" . | nindent 4 }}
